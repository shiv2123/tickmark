"""Evidence bundle: normalization, derivation, and digest.

Everything in this package is pure. No network, no clock, no filesystem, no
randomness (AGENTS.md rule 2). That is what makes the bundle reproducible and
the tests offline.
"""
